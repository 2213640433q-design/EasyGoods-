# 🔥 知识库调用问题 - 已修复

## 问题描述

**现象**：
- ✅ 在 Coze 平台网页端测试时，AI 可以正常调用知识库
- ❌ 通过 API 调用时，AI 不查询知识库，直接回答

## 根本原因

**缺少关键参数**：`with_knowledge: true`

Coze API 在调用时，需要**显式指定**是否启用知识库查询：
- 网页端测试：平台默认启用知识库
- API 调用：需要在请求体中添加 `with_knowledge: true`

## 修复方案

### ✅ 已修复的文件

#### 1. 云函数：`cloudfunctions/callCozeAgent/index.js`

**修改前**：
```javascript
const postData = JSON.stringify({
  conversation_id: conversationId,
  bot_id: COZE_CONFIG.botId,
  user: userId,
  query: userMessage,
  stream: false
})
```

**修改后**：
```javascript
const postData = JSON.stringify({
  conversation_id: conversationId,
  bot_id: COZE_CONFIG.botId,
  user: userId,
  query: userMessage,
  stream: false,
  auto_save_history: true, // 自动保存对话历史
  with_knowledge: true     // 🔥 关键：启用知识库查询！
})
```

#### 2. 测试脚本：`test-coze-api.js`

同样添加了 `with_knowledge: true` 参数。

---

## 📋 完整的 API 参数说明

### Coze v2 Chat API 关键参数

```javascript
{
  // ===== 必需参数 =====
  "bot_id": "your_bot_id",           // Bot ID
  "user": "user_identifier",          // 用户标识
  "query": "用户的问题",              // 用户消息
  
  // ===== 会话管理 =====
  "conversation_id": "unique_conv_id", // 会话 ID（保持对话连续性）
  
  // ===== 功能开关 =====
  "stream": false,                    // 是否流式返回
  "auto_save_history": true,          // 是否自动保存对话历史
  
  // ===== 🔥 知识库相关（重要！）=====
  "with_knowledge": true              // 是否启用知识库查询
}
```

---

## 🎯 验证修复

### 方法 1：本地测试

运行测试脚本：
```bash
cd /Users/Zhuanz/WeChatProjects/EasyGoods-租赁
node test-coze-api.js
```

**预期结果**：
- AI 应该根据知识库内容回答
- 如果知识库有相机租赁信息，应该回答准确的价格和政策

### 方法 2：部署后测试

1. **重新部署云函数**：
   - 右键 `callCozeAgent` 文件夹
   - 选择「上传并部署：云端安装依赖」

2. **在小程序测试**：
   - 进入客服页面
   - 询问知识库中的问题（如"相机租金多少？"）
   - 观察 AI 是否使用知识库回答

---

## 🔍 故障排查

如果添加参数后仍然不调用知识库，检查：

### ✅ Coze 平台配置

1. **知识库是否创建**：
   - 登录 Coze 平台
   - 进入 Bot 设置
   - 确认「知识库」已创建

2. **知识库是否关联到 Bot**：
   - 在 Bot 设置中
   - 确认知识库已启用并关联

3. **知识库内容是否生效**：
   - 查看知识库内容
   - 确认已保存并索引完成

### ✅ API 请求检查

查看云函数日志，确认请求体包含：
```json
{
  "with_knowledge": true,
  ...
}
```

### ✅ 知识库内容质量

- 知识库内容要**清晰、结构化**
- 问题和答案要**匹配用户常见提问方式**
- 避免内容过于分散

---

## 💡 优化建议

### 1. 知识库内容结构化

使用清晰的标题和分类：
```markdown
# 商品租赁价格

## 相机类设备
- Sony A7III: £5/天
- Canon EOS R5: £8/天

## 租期说明
- 最短租期：1天
- 长期租赁：7天以上享9折优惠
```

### 2. 添加常见问题

在知识库中包含用户常问的问题和标准答案：
```markdown
Q: 相机租金多少？
A: 相机租金按设备型号不同，从 £5/天起。具体价格请查看商品详情。

Q: 押金什么时候退？
A: 归还设备并确认无损后 3-5 个工作日内退还。
```

### 3. 使用关键词优化

在知识库中添加同义词和关键词：
```markdown
价格/租金/费用/多少钱 → 都指向相同的价格信息
押金/保证金/预付款 → 都指向押金政策
```

---

## 📊 参数对比表

| 参数 | 默认值 | 说明 | 是否必需 |
|------|--------|------|---------|
| `bot_id` | 无 | Bot 标识 | ✅ 必需 |
| `user` | 无 | 用户标识 | ✅ 必需 |
| `query` | 无 | 用户消息 | ✅ 必需 |
| `conversation_id` | 无 | 会话 ID | ✅ 推荐（保持上下文） |
| `stream` | false | 流式返回 | ⭕ 可选 |
| `auto_save_history` | false | 保存历史 | ⭕ 推荐 |
| **`with_knowledge`** | **false** | **启用知识库** | **🔥 必需（否则不查询知识库）** |

---

## ✅ 修复完成清单

- [x] 云函数添加 `with_knowledge: true`
- [x] 测试脚本添加 `with_knowledge: true`
- [x] 创建修复说明文档
- [ ] 重新部署云函数
- [ ] 测试验证知识库调用
- [ ] 在 Coze 平台优化知识库内容

---

## 🎉 总结

**核心要点**：
1. ✅ **必须添加** `with_knowledge: true` 参数
2. ✅ 知识库需要在 Coze 平台正确配置
3. ✅ 知识库内容要结构化、易于检索

**下一步**：
1. 重新部署云函数
2. 测试知识库调用效果
3. 根据实际效果优化知识库内容

---

**修复时间**：2024-11-11  
**修复状态**：✅ 代码已修复，待部署验证

