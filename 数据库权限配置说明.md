# 📋 products 集合权限配置说明

## 🎯 问题
数据库无法写入 `sampleImages` 和 `introImage` 字段

## ✅ 解决方案

### 步骤1：进入数据权限设置

```
微信开发者工具
  → 云开发
  → 数据库
  → products 集合
  → 点击顶部 "数据权限" 标签
```

### 步骤2：修改权限规则

**开发测试环境（推荐）：**

选择：**所有用户可读，仅创建者及管理员可写**

或者

**临时测试方案：**

选择：**所有用户可读，所有用户可写**
（⚠️ 注意：生产环境不建议使用此设置）

### 步骤3：保存并生效

点击 "保存" 按钮

---

## 🔍 验证权限是否生效

### 方法1：在控制台手动修改

1. 进入 products 集合
2. 找到任意一条记录
3. 点击 "编辑"
4. 尝试添加一个新字段 `testField: "test"`
5. 如果能保存，说明权限正确

### 方法2：通过小程序测试

1. 进入小程序 "商品图片管理"
2. 选择商品并点击 "保存"
3. 查看控制台日志
4. 如果显示 "更新记录数: 1"，说明成功

---

## 📊 权限说明

| 权限模式 | 读取 | 写入 | 适用场景 |
|----------|------|------|----------|
| 仅创建者可读写 | 创建者 | 创建者 | 用户私密数据 |
| 所有用户可读，仅创建者可写 | 所有人 | 创建者 | 用户生成内容 |
| 所有用户可读，仅管理员可写 | 所有人 | 管理员 | ✅ **商品数据（推荐）** |
| 所有用户可读写 | 所有人 | 所有人 | 测试环境 |

---

## ⚠️ 重要提示

**云函数默认拥有管理员权限**，所以：
- ✅ 即使设置为"仅管理员可写"，云函数也能正常写入
- ✅ 这是最安全的生产环境配置

**如果权限设置为"仅创建者可写"：**
- ❌ 云函数无法修改其他用户创建的记录
- ❌ 会导致更新失败

---

## 🎉 配置完成后

1. 重新运行小程序
2. 进入商品图片管理
3. 点击保存
4. 刷新云数据库页面
5. 查看 `sampleImages` 和 `introImage` 字段是否出现 ✅

