# 添加托管图标说明

## 当前状态
闲置托管功能目前使用 📦 emoji 作为图标，而不是图片文件。

## 如果要使用图片图标

### 1. 准备图标文件
- **文件名**：`hosting.png`
- **尺寸**：64x64px（建议）或与其他图标保持一致
- **格式**：PNG（带透明背景）
- **风格**：扁平化、简约，与其他图标风格一致

### 2. 放置图标文件
将 `hosting.png` 放到以下目录：
```
/static/icons/rental/hosting.png
```

与其他图标放在同一目录：
```
/static/icons/rental/
├── verify.png      (实名认证)
├── deposit.png     (我的押金) 
├── service.png     (在线客服)
└── hosting.png     ← 新增的托管图标
```

### 3. 修改代码

修改 `/pages/profile/index.wxml`，第86-91行：

**当前代码（使用emoji）：**
```xml
<view class="trade-item" bindtap="onTrade" data-key="hosting">
  <view class="trade-icon-bg hosting">
    <text class="trade-icon-emoji">📦</text>
  </view>
  <text class="trade-text">闲置托管</text>
</view>
```

**修改为（使用图片）：**
```xml
<view class="trade-item" bindtap="onTrade" data-key="hosting">
  <view class="trade-icon-bg hosting">
    <image class="trade-icon-img" src="/static/icons/rental/hosting.png" mode="aspectFit"/>
  </view>
  <text class="trade-text">闲置托管</text>
</view>
```

### 4. 重新编译
保存文件后，在微信开发者工具中点击"编译"即可看到效果。

---

## 为什么现在用 emoji？

因为 `hosting.png` 文件不存在，为了避免：
- ❌ 图片加载失败
- ❌ 显示空白
- ❌ 影响用户体验

所以临时使用了 📦 emoji 作为占位符。

---

## emoji 的优缺点

### ✅ 优点
- 无需图片文件
- 不会加载失败
- 跨平台兼容性好
- 文件更小

### ❌ 缺点
- 不同设备显示效果可能不同
- 无法完全控制样式
- 与其他图标风格不统一

---

## 建议

如果想保持与其他功能图标一致的视觉风格，建议：
1. 设计一个托管主题的图标（如箱子、包裹、仓库等）
2. 保存为 `hosting.png`（64x64px，PNG格式）
3. 放到 `/static/icons/rental/` 目录
4. 按上述步骤修改代码

如果 emoji 效果满意，也可以保持现状不变。




