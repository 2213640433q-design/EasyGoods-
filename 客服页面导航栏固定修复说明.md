# 🔧 客服页面导航栏固定修复说明

## 问题描述

**现象**：
- 在客服页面点击输入框，键盘弹起时
- 顶部的"在线客服"导航栏消失或被顶出屏幕
- 用户无法看到返回按钮和标题

## 修复方案

### ✅ 已修复的内容

#### 1. **页面基础样式**
添加 `page` 样式，防止整页滚动：

```css
page {
  height: 100vh;
  overflow: hidden;
  background: #F5F5F7;
}
```

#### 2. **导航栏固定定位**
确保导航栏使用超高 z-index：

```css
.navbar {
  position: fixed;  /* 固定定位 */
  top: 0;
  z-index: 99999;   /* 超高层级 */
  /* height 动态设置为 totalNavHeight */
}
```

#### 3. **动态计算总高度**
在 JS 中计算导航栏总高度（包含状态栏）：

```javascript
const statusBarHeight = systemInfo.statusBarHeight || 0
const navBarHeight = 44
const totalNavHeight = statusBarHeight + navBarHeight

this.setData({
  totalNavHeight  // 如：状态栏44px + 导航44px = 88px
})
```

#### 4. **WXML 使用动态高度**
```html
<view class="navbar" style="padding-top: {{statusBarHeight}}px; height: {{totalNavHeight}}px;">
```

#### 5. **消息区域适配**
```html
<scroll-view style="padding-top: {{totalNavHeight}}px;">
```

#### 6. **层级管理**
```
导航栏：z-index: 99999  ← 最高，始终可见
输入区：z-index: 9998   ← 略低于导航栏
商品栏：z-index: 9997   ← 更低
对话框：z-index: 2000   ← 弹窗层
```

---

## 📊 修复前后对比

### 修复前
```
键盘弹起 → 页面整体上移 → 导航栏被顶出屏幕 ❌
```

### 修复后
```
键盘弹起 → 导航栏固定在顶部 → 始终可见 ✅
           消息区域正确显示 ✅
           输入区域上移到键盘上方 ✅
```

---

## 🎯 技术要点

### 关键 1：Fixed 定位 + 超高 z-index
```css
position: fixed;
top: 0;
z-index: 99999;
```

### 关键 2：动态高度计算
```javascript
totalNavHeight = statusBarHeight + 44
```

### 关键 3：为固定元素预留空间
```html
<scroll-view style="padding-top: {{totalNavHeight}}px;">
```

---

## ✅ 测试验证

### 测试步骤
1. 打开客服页面
2. 点击底部输入框
3. 键盘弹起
4. 检查顶部"在线客服"导航栏是否可见

### 预期结果
- ✅ 导航栏始终固定在屏幕顶部
- ✅ 返回按钮和标题清晰可见
- ✅ 消息区域不被遮挡
- ✅ 输入框在键盘上方

---

## 📝 相关文件

- `pages/customer-service/index.wxml` - 添加动态高度
- `pages/customer-service/index.js` - 计算 totalNavHeight
- `pages/customer-service/index.wxss` - 优化布局和层级

---

**修复时间**：2024-11-12  
**修复状态**：✅ 已完成


