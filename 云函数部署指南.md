# ☁️ 云函数部署指南

## 📋 必须部署的云函数

托管功能需要以下4个云函数：

1. ✅ `submitHostedDevice` - 提交托管设备
2. ✅ `getHostedDevices` - 获取设备列表
3. ✅ `getHostedStats` - 获取统计数据
4. ✅ `recallHostedDevice` - 召回设备

---

## 🚀 部署步骤（详细图文）

### 方法1：逐个上传（推荐新手）

#### 步骤1：展开云函数目录
1. 在微信开发者工具左侧项目目录中
2. 找到 `cloudfunctions` 文件夹
3. 点击展开

#### 步骤2：上传第一个云函数
1. **右键点击** `submitHostedDevice` 文件夹
2. 选择 **"上传并部署：云端安装依赖"**
3. 等待上传完成（约10-30秒）
4. 看到 ✅ 绿色勾号表示成功

#### 步骤3：依次上传其他云函数
重复步骤2，依次上传：
- `getHostedDevices`
- `getHostedStats`
- `recallHostedDevice`

---

### 方法2：批量上传（推荐熟手）

#### 步骤1：打开云开发控制台
1. 点击工具栏的 **"云开发"** 按钮
2. 进入云开发控制台

#### 步骤2：进入云函数管理
1. 点击左侧 **"云函数"** 菜单
2. 看到云函数列表

#### 步骤3：批量上传
1. 在项目中选中所有需要上传的云函数文件夹
2. 右键选择 **"上传并部署：云端安装依赖"**

---

## ✅ 验证部署是否成功

### 方法1：查看云开发控制台
1. 打开云开发控制台
2. 点击"云函数"菜单
3. 查看列表中是否有这4个云函数
4. 状态应该显示为 **"部署成功"** 或 **"正常"**

### 方法2：测试云函数
1. 在云函数列表中，点击云函数名称
2. 点击 **"测试"** 按钮
3. 输入测试参数：
```json
{
  "subCategory": "CCD",
  "deviceName": "测试相机",
  "originalPrice": 1000,
  "condition": "九成新",
  "accessories": "充电器、电池",
  "images": ["test.jpg"],
  "depositRatio": 100,
  "dailyRent": 10
}
```
4. 点击 **"运行测试"**
5. 如果返回 `{"success": true, ...}` 则成功

---

## ❌ 常见错误及解决

### 错误1：`FunctionName parameter could not be found`
**原因**：云函数未上传
**解决**：按上述步骤上传云函数

### 错误2：`errCode: -404`
**原因**：云函数不存在
**解决**：检查云函数名称是否正确，重新上传

### 错误3：`dependency install fail`
**原因**：依赖安装失败
**解决**：
1. 检查网络连接
2. 重新选择 "上传并部署：云端安装依赖"
3. 如果仍然失败，手动进入云函数目录运行 `npm install`

### 错误4：`timeout`
**原因**：网络超时或云函数执行时间过长
**解决**：
1. 检查网络连接
2. 稍后重试
3. 增加云函数超时时间（在 config.json 中设置）

---

## 📊 部署完成后的测试

### 1. 创建数据库集合
在云开发控制台：
1. 点击 **"数据库"** 菜单
2. 点击 **"添加集合"**
3. 集合名称：`hosted_devices`
4. 权限设置：**仅创建者可读写**
5. 点击 **"确定"**

### 2. 测试完整流程
1. 在小程序中，进入"我的" → "闲置托管"
2. 选择设备类型（如"CCD相机"）
3. 填写设备信息（名称、价格、状态等）
4. 上传至少1张照片
5. 选择押金比例和设置租金
6. 点击 **"提交审核"**
7. 如果提示 **"提交成功"**，则说明云函数正常工作

### 3. 验证数据库
1. 打开云开发控制台 → 数据库
2. 点击 `hosted_devices` 集合
3. 查看是否有新增的数据记录

---

## 🎯 部署检查清单

- [ ] 上传 `submitHostedDevice` 云函数
- [ ] 上传 `getHostedDevices` 云函数
- [ ] 上传 `getHostedStats` 云函数
- [ ] 上传 `recallHostedDevice` 云函数
- [ ] 创建 `hosted_devices` 数据库集合
- [ ] 测试提交托管设备功能
- [ ] 验证数据是否正确保存到数据库

---

## 📞 需要帮助？

如果遇到问题：
1. 查看微信开发者工具控制台的错误信息
2. 查看云函数日志（云开发控制台 → 云函数 → 日志）
3. 检查数据库权限设置
4. 确认云开发环境已正确初始化

---

**部署完成后，闲置托管功能即可正常使用！** 🎉




